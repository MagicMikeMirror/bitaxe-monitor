# Security

Please report security issues privately through GitHub's security advisory
feature instead of opening a public issue.

The monitor uses an explicit allow-list before telemetry reaches SQLite or the
dashboard. It never stores the raw AxeOS response, wallet addresses, pool URLs,
pool users, pool passwords, Wi-Fi SSIDs, MAC addresses or device IP addresses.
